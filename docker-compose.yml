services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dodge
      POSTGRES_USER: dodge
      POSTGRES_PASSWORD: dodge
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
